Docker compose build para container que tenham custom Docker File
```bash
docker compose build
```

Subir o docker compose

```bash
docker compose up
```

Executar e já fazer build da imagem
```bash
docker compose up --build
```


```bash
docker compose down
```
